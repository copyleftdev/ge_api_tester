[pytest]
testpaths = api/tests harness/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Uncomment when pytest-cov is installed
# addopts = --cov=api --cov=harness --cov-report=term-missing
